import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Planea smoke test', (WidgetTester tester) async {
    // Smoke test placeholder – Firebase must be initialized before running full tests.
    expect(true, isTrue);
  });
}
